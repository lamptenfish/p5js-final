class Line{
    constructor(x,strokew){
        this.x=x;
        this.strokew=strokew;
        this.dir=1;
    }

    show(){
        strokeWeight(this.strokew);
        if (700>=x>0){
            y1= -6/7x+800
        }
        else if (900>=x>700){
            y1=
        }
        else {
            y1=
        }
        
        lines(this.x,this.y1,this.x,height);
    }

    ifTurn(){
        return this
    }
}